## Rust now supports the `has_define` method

This method returns true if the given cfg (either a valueless symbol, or
`name="value"`) is defined, else false is returned. Rust does not support
`get_define`.

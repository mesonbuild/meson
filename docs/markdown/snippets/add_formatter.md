## Add `fmt_unstable` command line option

This is an experimental formatter for meson.build files. It is unstable,
as some inline comments won't be added or will be added at a completely
wrong location.

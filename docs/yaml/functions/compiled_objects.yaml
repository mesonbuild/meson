name: compiled_objects
returns: compiled_objs
since: 0.61.0
description: Compiles objects with the given sources.

posargs_inherit: _build_target_base
varargs_inherit: _build_target_base
kwargs_inherit: _build_target_base

  pic:
    type: bool
    since: 0.61.0
    description: Compile objects as position-independent code.

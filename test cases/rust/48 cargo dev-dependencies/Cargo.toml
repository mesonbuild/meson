[workspace]
resolver = "2"
members = [".", "mylib", "testutil"]
default-members = ["."]

[package]
name = "app"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
mylib = { path = "mylib" }

[dev-dependencies]
testutil = { path = "testutil" }

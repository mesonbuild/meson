{
  "matrix": {
    "options": {
      "method": [
        { "val": "pkg-config" },
        { "val": "config-tool" }
      ]
    }
  },
  "expect_skip_on_jobname": ["azure", "cygwin", "fedora", "msys2", "opensuse", "windows"]
}

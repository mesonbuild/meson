project('file object', 'c')

top_dir = path()
include_dir = path('include')

prog0 = files('prog.c')
lib0 = files('lib.c')
test('fobj', executable('fobj', prog0, lib0))

subdir('subdir1')
subdir('subdir2')

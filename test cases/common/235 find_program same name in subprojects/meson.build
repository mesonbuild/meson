# SPDX-license-identifier: Apache-2.0

# If a script is found in a subproject, it should not be retrieved as cached in
# another subproject.

project('find_program same name differnt subprojects')

subproject('sub1')
subproject('sub2')